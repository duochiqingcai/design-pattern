代理模式，控制和管理访问。提供对目标对象另外的访问方法是；即通过代理对象访问目标对象，这样做可以在目标对象实现的基础上，增强额外的功能操作，即拓展目标的功能。

按代理创建时期不同分为静态代理和动态代理；
静态代理Static Proxy是由程序员创建或特定工具自动生成源代码，在对其编译，在程序员运行之前，代理类.class文件就已经被创建了。
动态代理Dynamic Proxy是在程序运行时通过反射机制动态创建的。

比如明星都有经纪人负责管理对外的业务，这就是代理模式的体现。